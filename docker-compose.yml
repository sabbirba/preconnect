services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        GITHUB_REPO: sabbirba/preconnect
    container_name: preconnect-web
    restart: always
    expose:
      - "80"
