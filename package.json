{
  "name": "preconnect-web",
  "version": "1.0.0",
  "description": "PreConnect Web App & Worker Proxy",
  "main": "index.js",
  "scripts": {
    "build": "bash tool/build_cloudflare.sh",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.60.0"
  }
}
