import 'package:flutter/material.dart';

class SemesterCalendarPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Semester Calendar', style: TextStyle(fontSize: 24)),
    );
  }
}
