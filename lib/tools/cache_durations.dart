class CacheDurations {
  CacheDurations._();

  static const Duration short = Duration(seconds: 2);
  static const Duration notification = Duration(seconds: 10);
  static const Duration profileOverview = Duration(seconds: 15);
  static const Duration notificationBadge = Duration(seconds: 30);
}
