name: Release

on:
  push:
    paths:
      - "**.dart"
    branches:
      - main
  workflow_dispatch:
  schedule:
    # Promotion to Production runs every Friday at 12:00 AM Bangladesh time (Thursday 18:00 UTC).
    - cron: "00 18 * * 4"

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  detect-pending:
    name: Detect Changes
    runs-on: ubuntu-latest
    outputs:
      should_run: ${{ steps.check.outputs.should_run }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Check for unreleased commits
        id: check
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "should_run=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          HEAD_AUTHOR="$(git log -1 --pretty=format:%an)"
          HEAD_EMAIL="$(git log -1 --pretty=format:%ae)"
          HEAD_SUBJECT="$(git log -1 --pretty=format:%s)"
          if [ "$HEAD_AUTHOR" = "github-actions[bot]" ] && \
             [ "$HEAD_EMAIL" = "github-actions[bot]@users.noreply.github.com" ] && \
             [[ "$HEAD_SUBJECT" == chore:\ bump\ version\ to* ]]; then
            echo "Latest commit is the release bot's version bump; skipping retrigger."
            echo "should_run=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          LAST_TAG=$(git tag --list 'v*' --sort=-v:refname | head -n1 || true)
          if [ -z "$LAST_TAG" ]; then
            echo "No release tag found yet; scheduled run should proceed."
            echo "should_run=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          LAST_TAG_SHA=$(git rev-list -n 1 "$LAST_TAG")
          HEAD_SHA=$(git rev-parse HEAD)
          if [ "$HEAD_SHA" = "$LAST_TAG_SHA" ]; then
            echo "No new commits since $LAST_TAG; scheduled release is not required."
            echo "should_run=false" >> "$GITHUB_OUTPUT"
          else
            echo "New commits found since $LAST_TAG; scheduled release should proceed."
            echo "should_run=true" >> "$GITHUB_OUTPUT"
          fi

  prepare-version:
    name: Prepare Version
    if: ${{ github.event_name == 'workflow_dispatch' || needs.detect-pending.outputs.should_run == 'true' }}
    needs: detect-pending
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      version_name: ${{ steps.version.outputs.version_name }}
      version_code: ${{ steps.version.outputs.version_code }}
      release_sha: ${{ github.sha }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Auto Bump Version
        shell: bash
        run: |
          mapfile -t VERSION_PARTS < <(./tool/sync_versions.sh bump-release)
          VERSION_NAME="${VERSION_PARTS[0]}"
          VERSION_CODE="${VERSION_PARTS[1]}"
          echo "Updated pubspec version to ${VERSION_NAME}+${VERSION_CODE}"

      - name: Read Version
        id: version
        shell: bash
        run: |
          mapfile -t VERSION_PARTS < <(./tool/sync_versions.sh read)
          VERSION_NAME="${VERSION_PARTS[0]}"
          VERSION_CODE="${VERSION_PARTS[1]}"
          echo "version_name=$VERSION_NAME" >> "$GITHUB_OUTPUT"
          echo "version_code=$VERSION_CODE" >> "$GITHUB_OUTPUT"

  create-release:
    name: Create Release
    if: ${{ github.event_name == 'workflow_dispatch' || needs.detect-pending.outputs.should_run == 'true' }}
    runs-on: ubuntu-latest
    needs: [detect-pending, prepare-version]
    permissions:
      contents: write
    outputs:
      release_tag: ${{ steps.meta.outputs.release_tag }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ needs.prepare-version.outputs.release_sha }}
          fetch-depth: 0

      - name: Prepare Release Metadata
        id: meta
        shell: bash
        run: |
          RELEASE_TAG="v${{ needs.prepare-version.outputs.version_name }}+${{ needs.prepare-version.outputs.version_code }}"
          echo "release_tag=$RELEASE_TAG" >> "$GITHUB_OUTPUT"

          LAST_TAG=$(git tag --list 'v*' --sort=-v:refname | head -n1 || true)
          if [ -n "$LAST_TAG" ]; then
            RANGE="$LAST_TAG..HEAD"
          else
            RANGE="HEAD"
          fi

          NOTES_FILE="release-notes.md"
          {
            echo "# PreConnect Release Notes"
            echo
            echo "## Version"
            echo "- Version: ${{ needs.prepare-version.outputs.version_name }}+${{ needs.prepare-version.outputs.version_code }}"
            echo "- Commit: ${{ needs.prepare-version.outputs.release_sha }}"
            echo
            echo "## Changes"
            if [ "$RANGE" = "HEAD" ]; then
              git log --max-count=100 --pretty=format:'- %s (%h)'
            else
              git log "$RANGE" --pretty=format:'- %s (%h)'
            fi
            echo
          } > "$NOTES_FILE"

      - name: Create GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          gh release create "${{ steps.meta.outputs.release_tag }}" \
            --title "PreConnect ${{ steps.meta.outputs.release_tag }}" \
            --notes-file release-notes.md \
            --target "${{ needs.prepare-version.outputs.release_sha }}" || \
          gh release edit "${{ steps.meta.outputs.release_tag }}" \
            --title "PreConnect ${{ steps.meta.outputs.release_tag }}" \
            --notes-file release-notes.md \
            --target "${{ needs.prepare-version.outputs.release_sha }}"

  build:
    name: Build ${{ matrix.name }}
    if: ${{ github.event_name == 'workflow_dispatch' || needs.detect-pending.outputs.should_run == 'true' }}
    needs: [detect-pending, prepare-version, create-release]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    permissions:
      contents: write
    env:
      DEVELOPMENT_TEAM: ${{ secrets.DEVELOPMENT_TEAM }}
      storeFile: preconnect-release-key.jks
      storePassword: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
      keyAlias: ${{ secrets.ANDROID_KEYSTORE_ALIAS }}
      keyPassword: ${{ secrets.ANDROID_KEY_PASSWORD }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            platform: chrome_extension
            name: Chrome Extension
          - os: ubuntu-latest
            platform: web
            name: Web
          - os: ubuntu-22.04
            platform: android
            name: Android
          - os: macos-latest
            platform: ios
            name: iOS
          - os: macos-latest
            platform: macos
            name: macOS

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ needs.prepare-version.outputs.release_sha }}
          fetch-depth: 0

      - name: Auto Bump Version
        shell: bash
        run: |
          ./tool/sync_versions.sh bump-release

      - name: Setup Java
        if: matrix.platform == 'android'
        uses: actions/setup-java@v5.2.0
        with:
          distribution: "temurin"
          java-version: "17"

      - name: Setup Flutter
        uses: subosito/flutter-action@v2.23.0
        with:
          channel: "stable"
          cache: true

      - name: Flutter Version
        run: flutter --version

      - name: Setup Xcode
        if: matrix.platform == 'ios' || matrix.platform == 'macos'
        uses: maxim-lobanov/setup-xcode@v1.7.0
        with:
          xcode-version: "latest"

      - name: Show Xcode
        if: matrix.platform == 'ios' || matrix.platform == 'macos'
        run: |
          xcodebuild -version
          xcodebuild -showsdks

      - name: Cache Pub
        uses: actions/cache@v5.0.5
        with:
          path: |
            ~/.pub-cache
            C:\Users\runneradmin\AppData\Local\Pub\Cache
          key: ${{ runner.os }}-flutter-pub-${{ hashFiles('**/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-flutter-pub-

      - name: Cache Gradle
        if: matrix.platform == 'android'
        uses: actions/cache@v5.0.5
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('android/**/build.gradle*', 'android/**/settings.gradle*', 'android/gradle.properties', 'android/gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Android Props
        if: matrix.platform == 'android'
        shell: bash
        run: |
          {
            printf 'flutter.sdk=%s\n' "$FLUTTER_ROOT"
            if [ -n "${ANDROID_SDK_ROOT:-}" ]; then
              printf 'sdk.dir=%s\n' "$ANDROID_SDK_ROOT"
            elif [ -n "${ANDROID_HOME:-}" ]; then
              printf 'sdk.dir=%s\n' "$ANDROID_HOME"
            fi
            NDK_DIR="${ANDROID_NDK_HOME:-${ANDROID_NDK_ROOT:-}}"
            if [ -n "$NDK_DIR" ] && [ ! -d "$NDK_DIR" ]; then
              echo "Configured Android NDK path does not exist: ${NDK_DIR}" >&2
              exit 1
            fi
          } > android/local.properties

      - name: Refresh Gradle
        if: matrix.platform == 'android'
        shell: bash
        run: |
          set -euo pipefail
          cd android
          ./gradlew --stop
          rm -rf .gradle/caches
          ./gradlew clean --no-daemon --stacktrace --info
          ./gradlew --no-daemon help --refresh-dependencies

      - name: Cache Build
        uses: actions/cache@v5.0.5
        with:
          path: |
            build
            .dart_tool
          key: ${{ runner.os }}-${{ matrix.platform }}-flutter-build-${{ hashFiles('pubspec.yaml', 'pubspec.lock', 'android/build.gradle.kts', 'android/app/build.gradle.kts', 'android/settings.gradle.kts', 'android/gradle.properties', 'android/gradle/wrapper/gradle-wrapper.properties', 'tool/**', 'web/**', 'lib/**', 'ios/**', 'macos/**') }}
          restore-keys: |
            ${{ runner.os }}-${{ matrix.platform }}-flutter-build-

      - name: Flutter Pub Get
        shell: bash
        run: flutter pub get

      - name: Release Env
        shell: bash
        run: |
          cat > .env <<'EOF'
          GITHUB_TOKEN=${{ secrets.GH_TOKEN }}
          DEVELOPMENT_TEAM=${{ secrets.DEVELOPMENT_TEAM }}
          EOF

      - name: Chrome Extension
        if: matrix.platform == 'chrome_extension'
        shell: bash
        run: ./tool/build_extension.sh

      - name: Chrome Package
        if: matrix.platform == 'chrome_extension'
        run: |
          mkdir -p artifacts
          cd build/chrome-extension
          zip -r "$GITHUB_WORKSPACE/artifacts/PreConnect-chrome-extension-release-${{ needs.prepare-version.outputs.version_name }}+${{ needs.prepare-version.outputs.version_code }}.zip" .

      - name: Build Web
        if: matrix.platform == 'web'
        shell: bash
        run: |
          flutter build web --release --tree-shake-icons --csp --no-wasm-dry-run --no-web-resources-cdn --dart-define-from-file=.env

      - name: Web Package
        if: matrix.platform == 'web'
        run: |
          mkdir -p artifacts
          cd build/web
          zip -r "$GITHUB_WORKSPACE/artifacts/PreConnect-web-release-${{ needs.prepare-version.outputs.version_name }}+${{ needs.prepare-version.outputs.version_code }}.zip" .

      - name: Check VPS Secrets
        id: vps_check
        if: matrix.platform == 'web'
        shell: bash
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
        run: |
          if [ -n "$VPS_HOST" ]; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          else
            echo "enabled=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Deploy to VPS
        if: matrix.platform == 'web' && steps.vps_check.outputs.enabled == 'true'
        uses: appleboy/scp-action@v1.0.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT || '22' }}
          source: "build/web/*"
          target: "/var/www/preconnect-web"
          strip_components: 2

      - name: Clean Build
        if: matrix.platform == 'android' || matrix.platform == 'ios' || matrix.platform == 'macos'
        shell: bash
        run: |
          flutter clean
          if [ "${{ matrix.platform }}" = "android" ]; then
            cd android
            ./gradlew clean
          fi

      - name: Restore Keystore
        if: matrix.platform == 'android'
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEYSTORE_ALIAS: ${{ secrets.ANDROID_KEYSTORE_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          if [ -z "$ANDROID_KEYSTORE_BASE64" ] || [ -z "$ANDROID_KEYSTORE_PASSWORD" ] || [ -z "$ANDROID_KEYSTORE_ALIAS" ] || [ -z "$ANDROID_KEY_PASSWORD" ]; then
            echo "Android signing secrets missing; skipping keystore restore."
            exit 0
          fi
          echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 -d > android/preconnect-release-key.jks
          chmod 600 android/preconnect-release-key.jks
          cat > android/key.properties <<EOF
          storeFile=preconnect-release-key.jks
          storePassword=${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          keyAlias=${{ secrets.ANDROID_KEYSTORE_ALIAS }}
          keyPassword=${{ secrets.ANDROID_KEY_PASSWORD }}
          EOF

      - name: Android APK
        if: matrix.platform == 'android'
        shell: bash
        run: |
          flutter build apk --release --split-per-abi --target-platform android-arm,android-arm64 --tree-shake-icons --obfuscate --split-debug-info=build/symbols/android --extra-gen-snapshot-options=--strip --dart-define-from-file=.env --build-name ${{ needs.prepare-version.outputs.version_name }} --build-number ${{ needs.prepare-version.outputs.version_code }}

      - name: Android AAB
        if: matrix.platform == 'android'
        shell: bash
        run: |
          set -euo pipefail
          AAB_PATH="build/app/outputs/bundle/release/app-release.aab"
          if ! flutter build appbundle --release --target-platform android-arm,android-arm64 --tree-shake-icons --obfuscate --split-debug-info=build/symbols/android --extra-gen-snapshot-options=--strip --dart-define-from-file=.env --build-name ${{ needs.prepare-version.outputs.version_name }} --build-number ${{ needs.prepare-version.outputs.version_code }}; then
            echo "Flutter appbundle validation failed; retrying bundle packaging with Gradle." >&2
            cd android
            ./gradlew bundleRelease --no-daemon --stacktrace --info
          fi

      - name: iOS App
        if: matrix.platform == 'ios'
        shell: bash
        run: |
          flutter build ios --release --no-codesign --tree-shake-icons --obfuscate --split-debug-info=build/symbols/ios --extra-gen-snapshot-options=--strip --dart-define-from-file=.env
          APP_PATH=$(find build/ios/iphoneos -maxdepth 1 -type d -name "*.app" | head -n1)
          if [ -d "$APP_PATH" ]; then
            strip -x "$APP_PATH/Runner" || true
            strip -x "$APP_PATH/Frameworks/App.framework/App" || true
          fi

      - name: Detect Signing
        id: ios_signing
        if: matrix.platform == 'ios'
        shell: bash
        env:
          DEVELOPMENT_TEAM: ${{ secrets.DEVELOPMENT_TEAM }}
          IOS_P12_BASE64: ${{ secrets.IOS_P12_BASE64 }}
          IOS_P12_PASSWORD: ${{ secrets.IOS_P12_PASSWORD }}
          IOS_PROFILE_BASE64: ${{ secrets.IOS_PROFILE_BASE64 }}
        run: |
          if [ -n "$DEVELOPMENT_TEAM" ] && [ -n "$IOS_P12_BASE64" ] && \
             [ -n "$IOS_P12_PASSWORD" ] && [ -n "$IOS_PROFILE_BASE64" ]; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
            echo "iOS signing secrets detected; signed IPA export is enabled."
          else
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            echo "iOS signing secrets missing; continuing with unsigned iOS fallback."
          fi

      - name: iOS IPA
        if: matrix.platform == 'ios' && steps.ios_signing.outputs.enabled == 'true'
        env:
          IOS_P12_BASE64: ${{ secrets.IOS_P12_BASE64 }}
          IOS_P12_PASSWORD: ${{ secrets.IOS_P12_PASSWORD }}
          IOS_PROFILE_BASE64: ${{ secrets.IOS_PROFILE_BASE64 }}
          KEYCHAIN_PASSWORD: ${{ secrets.IOS_P12_PASSWORD }}
        shell: bash
        run: |
          set -euo pipefail
          if [ -z "$IOS_P12_BASE64" ] || [ -z "$IOS_P12_PASSWORD" ] || [ -z "$IOS_PROFILE_BASE64" ]; then
            echo "iOS signing secrets missing; signed IPA export disabled, continuing with unsigned artifact packaging."
            exit 0
          fi
          [ -f ios/ExportOptions.plist ] || { echo "Missing required file: ios/ExportOptions.plist"; exit 1; }
          KEYCHAIN_PATH=$RUNNER_TEMP/build.keychain
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          printf '%s' "$IOS_P12_BASE64" | base64 -d > "$RUNNER_TEMP/cert.p12"
          security import "$RUNNER_TEMP/cert.p12" -k "$KEYCHAIN_PATH" -P "$IOS_P12_PASSWORD" -T /usr/bin/codesign
          security list-keychains -s "$KEYCHAIN_PATH"
          mkdir -p "$HOME/Library/MobileDevice/Provisioning Profiles"
          printf '%s' "$IOS_PROFILE_BASE64" | base64 -d > "$HOME/Library/MobileDevice/Provisioning Profiles/profile.mobileprovision"
          flutter build ipa --release --tree-shake-icons --obfuscate --split-debug-info=build/symbols/ios --extra-gen-snapshot-options=--strip --dart-define-from-file=.env --export-options-plist=ios/ExportOptions.plist

      - name: macOS App
        if: matrix.platform == 'macos'
        shell: bash
        run: |
          flutter build macos --release --tree-shake-icons --dart-define-from-file=.env
          APP_PATH=$(find build/macos/Build/Products/Release -maxdepth 1 -type d -name "*.app" | head -n1)
          if [ -d "$APP_PATH" ]; then
            strip -x "$APP_PATH/Contents/Frameworks/App.framework/Versions/Current/App" || true
            strip -x "$APP_PATH/Contents/MacOS/PreConnect" || true
          fi

      - name: Android Package
        if: matrix.platform == 'android'
        run: |
          set -euo pipefail
          mkdir -p artifacts
          shopt -s nullglob
          apk_paths=(build/app/outputs/flutter-apk/app-*-release.apk)
          shopt -u nullglob
          if [ "${#apk_paths[@]}" -eq 0 ]; then
            echo "No ABI split APKs found in build/app/outputs/flutter-apk." >&2
            exit 1
          fi
          for apk_path in "${apk_paths[@]}"; do
            abi="$(basename "$apk_path" | sed -E 's/^app-(.*)-release\.apk$/\1/')"
            cp "$apk_path" \
              "artifacts/PreConnect-android-${abi}-release-${{ needs.prepare-version.outputs.version_name }}+${{ needs.prepare-version.outputs.version_code }}.apk"
          done
          cp build/app/outputs/bundle/release/app-release.aab \
            artifacts/PreConnect-android-release-${{ needs.prepare-version.outputs.version_name }}+${{ needs.prepare-version.outputs.version_code }}.aab

      - name: iOS Package
        if: matrix.platform == 'ios'
        run: |
          mkdir -p artifacts
          if ls build/ios/ipa/*.ipa >/dev/null 2>&1; then
            IPA_PATH=$(ls build/ios/ipa/*.ipa | head -n1)
            cp "$IPA_PATH" \
              "artifacts/PreConnect-ios-release-${{ needs.prepare-version.outputs.version_name }}+${{ needs.prepare-version.outputs.version_code }}.ipa"
            exit 0
          fi
          if [ ! -d build/ios/iphoneos ]; then
            echo "No iOS build output directory found"
            exit 0
          fi
          APP_PATH=$(find build/ios/iphoneos -maxdepth 1 -type d -name "*.app" | head -n1)
          if [ -z "$APP_PATH" ] || [ ! -d "$APP_PATH" ]; then
            echo "No iOS .ipa or .app found"
            exit 0
          fi
          IPA_NAME="PreConnect-ios-release-${{ needs.prepare-version.outputs.version_name }}+${{ needs.prepare-version.outputs.version_code }}-unsigned.ipa"
          rm -rf Payload
          mkdir -p Payload
          ditto "$APP_PATH" "Payload/$(basename "$APP_PATH")"
          (cd Payload && zip -r -9 -y "$GITHUB_WORKSPACE/artifacts/$IPA_NAME" .) || true

      - name: macOS Package
        if: matrix.platform == 'macos'
        run: |
          mkdir -p artifacts
          APP_PATH=$(find build/macos/Build/Products/Release -maxdepth 1 -type d -name "*.app" | head -n1)
          if [ -z "$APP_PATH" ] || [ ! -d "$APP_PATH" ]; then
            echo "No macOS .app found"
            exit 0
          fi
          DMG_NAME="PreConnect-macos-release-${{ needs.prepare-version.outputs.version_name }}+${{ needs.prepare-version.outputs.version_code }}.dmg"
          hdiutil create -volname "PreConnect" -srcfolder "$APP_PATH" -ov -format UDZO "$GITHUB_WORKSPACE/artifacts/$DMG_NAME"

      - name: Upload Artifacts
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.platform }}-artifacts
          path: artifacts/*
          if-no-files-found: ignore

      - name: Upload Assets
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          if [ -d artifacts ] && find artifacts -type f -print -quit | grep -q .; then
            gh release upload "${{ needs.create-release.outputs.release_tag }}" artifacts/* --clobber
          else
            echo "No packaged artifacts for ${{ matrix.platform }}."
          fi

  publish-google-play:
    name: Publish Google Play Release (Android)
    if: ${{ github.event_name == 'workflow_dispatch' || needs.detect-pending.outputs.should_run == 'true' }}
    runs-on: ubuntu-latest
    needs: [detect-pending, prepare-version, build]
    timeout-minutes: 20
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ needs.prepare-version.outputs.release_sha }}
          fetch-depth: 0

      - name: Read Package
        id: android_meta
        shell: bash
        run: |
          PACKAGE_NAME=$(grep -E 'applicationId\s*=' android/app/build.gradle.kts | head -n1 | sed -E 's/.*"([^"]+)".*/\1/')
          echo "package_name=$PACKAGE_NAME" >> "$GITHUB_OUTPUT"

      - name: Download Android artifacts
        uses: actions/download-artifact@v8
        with:
          name: android-artifacts
          path: artifacts/android

      - name: Resolve AAB
        id: aab
        shell: bash
        run: |
          AAB_FILE=$(find artifacts/android -type f -name "*.aab" | head -n1 || true)
          if [ -z "$AAB_FILE" ]; then
            echo "found=false" >> "$GITHUB_OUTPUT"
            echo "No .aab file found in downloaded artifacts."
            exit 0
          fi
          echo "found=true" >> "$GITHUB_OUTPUT"
          echo "file=$AAB_FILE" >> "$GITHUB_OUTPUT"
          echo "Using AAB: $AAB_FILE"

      - name: Check Credentials
        id: play_auth
        shell: bash
        env:
          GOOGLE_PLAY_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_PLAY_SERVICE_ACCOUNT_JSON }}
        run: |
          if [ -n "$GOOGLE_PLAY_SERVICE_ACCOUNT_JSON" ]; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          else
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            echo "GOOGLE_PLAY_SERVICE_ACCOUNT_JSON is not provided."
          fi

      - name: Validate Google Play publish prerequisites
        shell: bash
        run: |
          if [ "${{ steps.aab.outputs.found }}" != "true" ]; then
            echo "Android .aab artifact is missing. Ensure Android release build/signing succeeded."
            exit 1
          fi
          if [ "${{ steps.play_auth.outputs.enabled }}" != "true" ]; then
            echo "GOOGLE_PLAY_SERVICE_ACCOUNT_JSON is not provided; skipping Google Play upload."
          fi

      - name: Generate Play release notes from commits
        id: play_notes
        shell: bash
        run: |
          NOTES_DIR="fastlane/metadata/android"
          CHANGELOG_DIR="$NOTES_DIR/en-US/changelogs"
          rm -rf "$NOTES_DIR"
          mkdir -p "$CHANGELOG_DIR"

          # Read the first versioned section from CHANGELOG.md (skip [Unreleased]).
          # Falls back to the git commit message if CHANGELOG.md is absent or empty.
          if [ -f CHANGELOG.md ]; then
            NOTES="$(
              awk '
                /^## \[/ && !/\[Unreleased\]/ {
                  if (found) exit
                  found = 1
                  next
                }
                found && /^## \[/ { exit }
                found { print }
              ' CHANGELOG.md \
              | sed 's/^[-*] /• /' \
              | sed 's/^#\+[[:space:]]*//'
            )"
          fi

          if [ -z "$(printf '%s' "${NOTES:-}" | tr -d '[:space:]')" ]; then
            NOTES="$(git log -1 --pretty=%B)"
          fi

          if [ -z "$(printf '%s' "${NOTES:-}" | tr -d '[:space:]')" ]; then
            NOTES="PreConnect ${{ needs.prepare-version.outputs.version_name }}+${{ needs.prepare-version.outputs.version_code }}"
          fi

          NOTES=$(printf '%s\n' "$NOTES" | awk -v max=480 '
            BEGIN { len = 0 }
            {
              add = length($0) + (len > 0 ? 1 : 0)
              if (len + add <= max) {
                if (len > 0) printf "\n"
                printf "%s", $0
                len += add
              } else {
                exit
              }
            }
            END {
              if (len > 0) printf "\n"
            }
          ')

          printf '%s' "$NOTES" > "$CHANGELOG_DIR/default.txt"
          printf '%s' "$NOTES" > "$CHANGELOG_DIR/${{ needs.prepare-version.outputs.version_code }}.txt"

          echo "dir=$NOTES_DIR" >> "$GITHUB_OUTPUT"


      - name: Setup Ruby
        if: steps.aab.outputs.found == 'true' && steps.play_auth.outputs.enabled == 'true'
        uses: ruby/setup-ruby@v1.313.0
        with:
          ruby-version: "3.3"
          bundler-cache: false

      - name: Install Ruby Gems
        if: steps.aab.outputs.found == 'true' && steps.play_auth.outputs.enabled == 'true'
        run: |
          gem install bundler --no-document
          bundle config set frozen false
          bundle install

      - name: Upload Internal
        if: steps.aab.outputs.found == 'true' && steps.play_auth.outputs.enabled == 'true'
        shell: bash
        env:
          GOOGLE_PLAY_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_PLAY_SERVICE_ACCOUNT_JSON }}
          FASTLANE_OPT_OUT_USAGE: "1"
        run: |
          bundle exec fastlane supply \
            --aab "${{ steps.aab.outputs.file }}" \
            --package_name "${{ steps.android_meta.outputs.package_name }}" \
            --json_key_data "$GOOGLE_PLAY_SERVICE_ACCOUNT_JSON" \
            --track "internal" \
            --release_status "completed" \
            --version_name "PreConnect v${{ needs.prepare-version.outputs.version_name }}+${{ needs.prepare-version.outputs.version_code }}" \
            --metadata_path "${{ steps.play_notes.outputs.dir }}" \
            --skip_upload_metadata true \
            --skip_upload_changelogs false \
            --skip_upload_images true \
            --skip_upload_screenshots true

      - name: Promote Beta
        if: steps.aab.outputs.found == 'true' && steps.play_auth.outputs.enabled == 'true'
        shell: bash
        env:
          GOOGLE_PLAY_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_PLAY_SERVICE_ACCOUNT_JSON }}
          FASTLANE_OPT_OUT_USAGE: "1"
        run: |
          bundle exec fastlane supply \
            --package_name "${{ steps.android_meta.outputs.package_name }}" \
            --json_key_data "$GOOGLE_PLAY_SERVICE_ACCOUNT_JSON" \
            --track "internal" \
            --track_promote_to "beta" \
            --track_promote_release_status "completed" \
            --skip_upload_apk true \
            --skip_upload_aab true \
            --skip_upload_metadata true \
            --skip_upload_changelogs true \
            --skip_upload_images true \
            --skip_upload_screenshots true
